{
  "name": "hitop-builder-smoke",
  "private": true,
  "version": "0.0.0",
  "description": "Headless smoke test for the HiTOP-SR Module Builder page. Nothing here is served: the Pages workflow publishes index.html, LICENSE.md and README.md and nothing else.",
  "type": "module",
  "scripts": {
    "smoke": "playwright test tests/smoke.spec.js",
    "runtime-probes": "playwright test tests/runtime-timeout.spec.js",
    "plants": "node tests/plants.mjs"
  },
  "devDependencies": {
    "@playwright/test": "1.56.1"
  }
}
